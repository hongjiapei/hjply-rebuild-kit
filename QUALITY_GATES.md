# HJPLY 优化终止规范

本规范用于回答“项目是否还需要继续优化”。目标不是无限寻找可改之处，而是在功能、风险、体积和可维护性达到明确门槛后停止改动。

## 1. 优化项分级

每个建议必须归入以下一种类型：

- **必须修复**：可复现的核心功能故障、崩溃、数据或凭据泄露、后门、构建失败、升级失败，或 Android/Cloudflare 的明确兼容性问题。
- **达到阈值才修复**：有测量基线、预期收益和验证方法，并达到本规范第 3 节的收益阈值。
- **不处理**：主观偏好、纯代码风格、没有数据支撑的性能猜测、只减少少量代码但增加风险的重构，以及超出本项目架构能力的问题。

不能说明问题证据、预期收益和验证方法的建议，不算有效优化项。

## 2. 发布硬门槛

以下项目必须全部通过，否则不能宣布优化结束。

### 2.1 Android 核心流程

- 可以使用同一签名覆盖安装上一版本。
- 首次安装能够获取配置、连接、断开并再次连接。
- 升级安装能够刷新配置版本，不要求用户清除数据或重新授权 VPN。
- 已有缓存配置时，订阅接口临时不可用不会删除可用配置。
- 中国大陆域名和 IP 直连，其他流量默认代理。
- Google HTTPS 可以通过 VLESS 打开；连续连接、断开至少 3 次无异常。
- 页面在状态栏和底部导航栏安全区内显示，不遮挡系统 UI。
- 目标最小布局为宽 `360dp`、高 `720dp`、系统字体比例 `1.0`；主要连接页无需滚动即可完成连接和断开。

### 2.2 Android 自动检查

- `testOtherDebugUnitTest`：0 失败。
- `lintOtherRelease`：0 error。
- `spotlessCheck`：通过。
- Release APK 在 R8 和资源压缩开启的情况下构建成功。
- APK 通过 `apksigner verify` 和 `zipalign -c -P 16 4`。
- APK 中包含 `geoip-cn.srs`、`geosite-cn.srs` 和预期 DNS 策略。
- Manifest 不包含 Root、Shizuku、Xposed、配置导入或其他已删除功能。

### 2.3 Worker 检查

- `npm run check` 全部通过。
- `npm audit` 为 0 个已知漏洞。
- Wrangler dry-run 成功。
- 生产订阅接口返回 HTTP 200，错误令牌不会返回订阅。
- 普通 VLESS 首包和拆分首包测试通过。
- 至少对 Google 做一次真实 TLS/VLESS 测试，而不是只测试 WebSocket 握手。
- Worker 代码中不得使用外部 ProxyIP、第三方订阅、未知回退服务器或遥测上报。

### 2.4 隐私与发布

- UUID、订阅令牌、APK 签名文件、签名密码、Cloudflare 凭据和生产 Wrangler 配置均未被 Git 跟踪。
- `git diff --check` 通过。
- README 能让新的部署者从空白 Cloudflare 账号完成部署和构建。
- 已知 Cloudflare Worker 出站限制必须写入 README，不能把架构限制描述成待修 Bug。

## 3. 有收益才允许继续优化

通过硬门槛后，新的非 Bug 优化至少满足一项才实施：

- APK 减少至少 `300 KiB` 或 `2%`，且不删除必要能力。
- 在同一设备、同一网络、至少 5 次测试下，启动时间、峰值内存、CPU 或连接耗时改善至少 `10%`。
- 删除一个不再使用的直接依赖、危险权限或外部网络依赖。
- 消除一个有明确复现步骤的竞态、资源泄漏或兼容性风险。
- 修复公开安全公告、依赖漏洞或平台弃用要求。

低于阈值的微小收益不实施，除非改动几乎没有风险且能明显降低维护成本。

## 4. 体积与依赖边界

- 当前 `arm64-v8a` Release APK 的体积上限为 `15 MiB`。
- 新版本若增加超过 `300 KiB`，必须在发布说明中解释原因。
- `libbox.so` 是核心运行库，不以二次压缩、删除符号以外的非标准方式处理；只有上游安全修复或明确功能需要时才升级。
- 不为了追求文件数量或代码行数而重写稳定逻辑。
- 不新增 Root、Shizuku、Xposed、二维码、配置编辑、应用更新或多页面框架，除非产品需求明确改变。

## 5. 停止条件

一次优化周期最多进行两轮：

1. 第一轮：发现并修复“必须修复”项和达到收益阈值的项目。
2. 第二轮：运行全部发布门槛并做一次回归审查。

满足以下条件后必须停止：

- 第 2 节全部通过。
- 第二轮没有新的“必须修复”项。
- 剩余建议均低于第 3 节收益阈值，或属于已记录的架构限制。
- APK 体积未超过第 4 节上限。

停止后，不再因为“理论上还能优化”而继续改代码。

## 6. 重新开启优化的条件

只有出现以下事件之一，才开启新的优化周期：

- 用户提供可复现故障或真机回归结果。
- APK 体积、启动时间、内存或连接性能超过既定阈值。
- Android、Cloudflare、Gradle、依赖库发生破坏性变化或安全公告。
- 明确新增或改变产品需求。
- 自动测试、线上探测或发布检查失败。

## 7. AI 检查输出格式

以后 AI 被要求“再看有没有可优化的点”时，只能按以下格式回答：

```text
结论：达标 / 不达标
必须修复：列出证据、影响和验证方法；没有则写“无”
达到阈值的优化：列出测量数据；没有则写“无”
不处理项：列出低收益建议或架构限制
停止判定：继续一轮 / 本版本结束
```

不得只回答“还可以继续优化”，也不得把未测量的猜测列为待办事项。

## 8. 当前基线

`1.0.22 (23)` 是当前验收基线：

- APK：`14.11 MiB`，低于 `15 MiB` 上限。
- 单页 UI 已适配系统安全区。
- 中国大陆直连，其他流量代理，远程 DNS 使用 `ipv4_only`。
- 页面、快捷设置、开机和应用更新恢复连接时，均等待配置初始化完成后再启动 VPN。
- Android 单元测试、Lint、格式检查、Release 构建、签名和对齐检查通过。
- Worker 单元测试、依赖审计、部署检查、Google TLS/VLESS 实测通过。
- 真机已确认 UI 和 Google 访问问题修复。

按本规范，`1.0.22` 已达到优化终止条件。Cloudflare 托管目标站点的访问限制属于已知架构边界，不作为继续重构客户端或订阅格式的理由。
